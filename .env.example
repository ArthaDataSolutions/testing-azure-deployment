# .env.example — generated by ETL Migration
# Baseline variables match the bundled Boomi sample processes;
# additional variables below were discovered from your specific process XML.

# === Baseline ==========================================================
# CRM REST base URL
CRM_API_BASE_URL=
# CRM OAuth2 token endpoint
CRM_OAUTH_TOKEN_URL=
# CRM OAuth client id
CRM_CLIENT_ID=
# CRM OAuth client secret
CRM_CLIENT_SECRET=
# CRM OAuth scope
CRM_OAUTH_SCOPE=
# Default lookback timestamp (ISO-8601)
CRM_LAST_RUN_DEFAULT_ISO=
# ERP REST base URL
ERP_BASE_URL=
# ERP HTTP basic auth user
ERP_BASIC_USER=
# ERP HTTP basic auth password
ERP_BASIC_PASSWORD=
# Storefront REST base URL
STOREFRONT_BASE_URL=
# Storefront OAuth2 token endpoint
STOREFRONT_OAUTH_TOKEN_URL=
# Storefront OAuth client id
STOREFRONT_OAUTH_CLIENT_ID=
# Storefront OAuth client secret
STOREFRONT_OAUTH_CLIENT_SECRET=
# Inbound webhook listener base path
WS_LISTENER_BASE_PATH=
# SFTP host
SFTP_HOST=
# SFTP port (typically 22)
SFTP_PORT=
# SFTP user
SFTP_USER=
# Path to SFTP private key file
SFTP_PRIVATE_KEY=
# Remote directory to read/write
SFTP_REMOTE_DIR=
# Data warehouse Postgres host
DW_POSTGRES_HOST=
# Data warehouse Postgres port
DW_POSTGRES_PORT=
# Data warehouse database name
DW_POSTGRES_DB=
# Data warehouse user
DW_POSTGRES_USER=
# Data warehouse password
DW_POSTGRES_PASSWORD=
# Email address to receive process failure alerts
OPS_ALERT_EMAIL=
