# Migrated Boomi Jobs

> Auto-generated by **Boomi → Python** migration tool.

## Job inventory

  - `jobs/XmlToJson_demo/main.py`
  - `jobs/JsonToXml_demo/main.py`

## Run locally

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
# Fill .env with connector credentials and runtime values.
python runtime.py
```

Run a single job by name:

```bash
python runtime.py --list
python runtime.py --job <job-name>
```

## Docker

```bash
docker-compose up --build
```
