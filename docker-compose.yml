version: "3.9"
services:
  pipeline:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    command: >
      sh -c "pip install -r requirements.txt -q && python runtime.py"
    env_file:
      - .env
